<h1 class = "page-heading"><%= @page.title %></h1>

<br />
<div class='spacer'></div>
<section class = 'page-content'>
<%= @page.content.html_safe %>
</section>
<section class = 'page-record'>
	<% if @page.recordfies %>
		<% @page.reacordfies.each do |record| %>
			<%= record.title %>
			<%= record.description %>
		<% end %>
	<% end %>
</section>

