$('#stateform select').hide();
$('#state-status').show();
$('#icon-span').append("<%= j image_tag state_icon(@course.workflow_state), :class => 'state-icon' %>");
$('#current-state').html('<%= j @course.current_state.to_s %>');
var currentState = $('#current-state').text();
$('#update-date').html('<%= j @course.updated_at.to_s %>');
buttonChange();