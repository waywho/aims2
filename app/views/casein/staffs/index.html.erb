<!-- Scaffolding generated by Casein v5.1.1.5 -->

<h2>Staffs<%= casein_pagination_details @staffs %></h2>

<%= form_tag edit_multiple_casein_staffs_path, method: :post do %>
	<%= render :partial => 'casein/staffs/table', :locals => { :staffs => @staffs } %>

	<%= render :partial => 'casein/layouts/multi_btns' %>
<% end %>

<div class="text-center">
	<%= will_paginate @staffs %>
</div>

<%= content_for :sidebar do %>

	<li><%= link_to "#{casein_show_icon "plus-sign"}Add a new staff".html_safe, new_casein_staff_path %></li>
	Download Data: <%= link_to 'CSV', casein_staffs_path(format: 'csv') %> | 
	<%= link_to 'Excel', casein_staffs_path(format: 'xlsx') %>
	<br />
	<br />
	Import Data with CSV file:
	<br />
	<%= form_tag import_casein_staffs_path, multipart: true do %>
		<%= file_field_tag :file, accept: 'text/csv' %>
		<%= submit_tag "Import", :class => 'btn btn-xs btn-primary pull-right' %>
	<% end %>
<% end %>