<!-- Scaffolding generated by Casein v5.1.1.5 -->

<h2>Fee</h2>

<%= form_for @fee, :url => casein_fee_path(@fee) do |f| %>

	<%= render :partial => 'casein/fees/form', :locals => { :f => f } %>
	
	<p class="submits">
	  	<%= link_to 'Cancel', casein_fees_path, :class => 'btn btn-mini btn-danger' %>
		<%= render :partial => 'casein/layouts/approval_q_buttons', :locals => { :f => f } %>
	</p>
	</p>

<% end %>

<%= content_for :sidebar do %>
	<li><%= link_to "#{casein_show_icon "th-list"}Back to list".html_safe, casein_fees_path %></li>
	<%= simple_form_for @fee, :url => casein_fee_path(@fee), html: {:id => 'stateform'}, remote: true do |f| %>
		<%= render partial: 'casein/layouts/status_form', :locals => {:f => f, :record => @fee } %>
	<% end %>
<% end %>
