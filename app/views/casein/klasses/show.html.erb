<!-- Scaffolding generated by Casein v5.1.1.5 -->

<h2>Klass</h2>

<%= form_for @klass, :url => casein_klass_path(@klass) do |f| %>

	<%= render :partial => 'casein/klasses/form', :locals => { :f => f } %>
	
	<p class="submits">
		<%= link_to 'Cancel', casein_courses_path, :class => 'btn btn-mini btn-default' %>
		<%= render :partial => 'casein/layouts/approval_q_buttons', :locals => { :f => f } %>
	</p>

<% end %>

<%= content_for :sidebar do %>
	<li><%= link_to "#{casein_show_icon "th-list"}Back to list".html_safe, casein_klasses_path %></li>
	<%= simple_form_for @klass, :url => casein_klass_path(@klass), html: {:id => 'stateform'}, remote: true do |f| %>
		<%= render partial: 'casein/layouts/status_form', :locals => {:f => f, :record => @klass } %>
	<% end %>
<% end %>