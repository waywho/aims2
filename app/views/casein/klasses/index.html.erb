<!-- Scaffolding generated by Casein v5.1.1.5 -->

<h2>Klasses<%= casein_pagination_details @klasses %></h2>
<%= form_tag '#' do %>
	<%= render :partial => 'casein/klasses/table', :locals => { :klasses => @klasses } %>
	<%= submit_tag "Publish", name: 'publish', :class => 'btn btn-mini btn-primary publish pull-right' %>
<% end %>
<div class="text-center">
	<%= will_paginate @klasses %>
</div>

<%= content_for :sidebar do %>

	<li><%= link_to "#{casein_show_icon "plus-sign"}Add a new klass".html_safe, new_casein_klass_path %></li>

<% end %>