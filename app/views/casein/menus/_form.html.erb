<!-- Scaffolding generated by Casein v5.1.1.5 -->

<div class="row">
	<h5>Published pages</h5>
	<br />
	<p>
	<% @pages.each do |page| %>
	<div class="col-lg-12 col-md-12">
		<p>
		<%= check_box_tag 'page_ids[]', page.id, checked = false, { id: dom_id(page) } %>
		<%= page.title %>
 		</p>
	</div>
	<% end %>
	</p>
</div>

<div class="row">
	<div class="col-lg-12 col-md-12">
		<%= casein_collection_select f, f.object, 'menu', :parent_id, Menu.roots, :id, :name, include_blank: true, casein_label: 'Parent menu item' %>
	</div>
	<div class="col-lg-12 col-md-12">
		<%= casein_select f, f.object, :menu_type, Menu::TYPES, include_blank: true %>
	</div>
</div>

