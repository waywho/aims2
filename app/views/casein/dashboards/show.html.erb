<!-- Scaffolding generated by Casein v5.1.1.5 -->

<h2>View dashboard</h2>

<%= form_for @dashboard, :url => casein_dashboard_path(@dashboard) do |f| %>

	<%= render :partial => 'casein/dashboards/form', :locals => { :f => f } %>
	
	<p class="submits">
	  	<%= link_to 'Cancel', casein_dashboards_path, :class => 'btn btn-mini btn-danger' %>
		<%= render :partial => 'casein/layouts/approval_q_buttons', :locals => { :f => f } %>
	</p>
	</p>

<% end %>

<%= content_for :sidebar do %>
	<li><%= link_to "#{casein_show_icon "th-list"}Back to list".html_safe, casein_dashboards_path %></li>
	<%= simple_form_for @dashboard, :url => casein_dashboard_path(@dashboard), html: {:id => 'stateform'}, remote: true do |f| %>
		<%= render partial: 'casein/layouts/status_form', :locals => {:f => f, :record => @dashboard } %>
	<% end %>
<% end %>
