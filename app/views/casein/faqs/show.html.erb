<!-- Scaffolding generated by Casein v5.1.1.5 -->

<h2>View faq</h2>

<%= form_for @faq, :url => casein_faq_path(@faq) do |f| %>

	<%= render :partial => 'casein/faqs/form', :locals => { :f => f } %>
	
	<p class="submits">
	  	<%= link_to 'Cancel', casein_faqs_path, :class => 'btn btn-mini btn-danger' %>
		<%= render :partial => 'casein/layouts/approval_q_buttons', :locals => { :f => f } %>
	</p>
	</p>

<% end %>

<%= content_for :sidebar do %>
	<li><%= link_to "#{casein_show_icon "th-list"}Back to list".html_safe, casein_faqs_path %></li>
	<%= simple_form_for @faq, :url => casein_faq_path(@faq), html: {:id => 'stateform'}, remote: true do |f| %>
		<%= render partial: 'casein/layouts/status_form', :locals => {:f => f, :record => @faq } %>
	<% end %>
<% end %>
